# Repository Codex overlay

Use **FAST_PATH**: read only task-relevant files, implement one coherent batch, run the smallest useful check, and keep reports short. Reuse established decisions instead of re-deriving them.

Do not add extra subagents, worktrees, broad TDD/regression, or long documentation unless the task or material risk requires them.

For long work, finish atomic milestones and use `CODEX_CHECKPOINT.md` before context/usage limits.